plugins {
    id 'groovy'
}

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.distelli.graphql:graphql-apigen:2.1.0'
    testCompile 'junit:junit:4.12'
}
